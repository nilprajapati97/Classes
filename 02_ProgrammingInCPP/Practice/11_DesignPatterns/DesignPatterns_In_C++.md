-: 🔹 Creational Patterns 🔹
=================================
Create or control object instantiation.

✅ Singleton — Ensuring a class has only a single instance.
✅ Factory Method — Create objects without specifying their exact class.
✅ Abstract Factory — Create families of related or dependent products.
✅ Builder — Construct complex objects step by step.
✅ Prototype — Clone an existing object instead of creating a new one.

-: 🔹 Structural Patterns 🔹
Organize classes or objects to form larger structures while retaining flexibility.

✅ Adapter — Convert one interface to another (for compatibility).
✅ Composite — Create tree structures to enable uniform treatment of composite and leaf components.
✅ Proxy — Provides a placeholder or surrogate for another object.
✅ Decorator — Add responsibilities to an object dynamically.
✅ Bridge — Separate abstraction from implementation.
✅ Facet/Facade — Provides a simplified interface to a complex subsystem.
✅ Flyweight — Share fine-grained object state efficiently.

-: 🔹 Behavioral Patterns 🔹
Improve or enable interaction and communication between objects.

✅ Observer — Publish/Subscribe notifications to multiple dependents.
✅ Strategy — Select algorithm’s behavior at runtime.
✅ Command — Encapsulate requests as object (for queuing, logging, undo/redo).
✅ Chain of Responsibility — Pass requests along a chain of handlers.
✅ Mediator — Centralize complex communications between components.
✅ Iterator — Traverse elements in a collection gracefully.
✅ Memento — Save and restore object’s state.
✅ State — Alter behavior when object’s internal state evolves.
✅ Template Method — General algorithm structure with customizable steps.
✅ Visitor — Add operations to object structures without changing their classes.