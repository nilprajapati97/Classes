🔹Sequence Containers (ordered by insertion)🔹:
✅ std::array — Fixed size array
✅ std::vector — Dynamic array
✅ std::deque — Double-ended queue
✅ std::list — Doubly-linked list
✅ std::forward_list — Singly-linked list

🔹Associative Containers (ordered by key)🔹:
✅ std::set — Stores unique keys in a tree (typically a red-black tree)
✅ std::multiset — Stores non-unique keys in a tree
✅ std::map — Stores pairs (key, value) with unique keys (ordered)
✅ std::multimap — Stores pairs with non-unique keys (ordered)

🔹Unordered Associative Containers (hashed)🔹:
✅ std::unordered_set — Stores unique keys (hashed)
✅ std::unordered_multiset — Stores non-unique keys (hashed)
✅ std::unordered_map — Stores pairs (hashed) with unique keys
✅ std::unordered_multimap — Stores pairs (hashed) with non-unique keys

🔹Container Adapters (derived from other containers)🔹:
✅ std::stack — LIFO stack (adapter; typically from std::deque)
✅ std::queue — FIFO queue (adapter; typically from std::deque)
✅ std::priority_queue — Priority queue (adapter; typically from std::vector)

